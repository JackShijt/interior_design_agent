# 已知限制清单（第 7 周联调后）

> 状态：MVP 原型已跑通 `识别→规划→生成→约束→对话→算量` 全链路（pytest 25 passed）。
> 以下为当前未解决/需进一步投入的场景，按优先级标注 P0/P1/P2。

## P0（影响安全/核心可用，需优先处理）
- **户型识别为 Mock**：当前 `recognize()` 返回内置示例户型。接外部识别 API 的 `_call_external_api` 为 TODO，未实现真实 CV。生产前必须接入（现成 API 优先）。
- **承重墙判定依赖原始数据**：`original_walls` 基线来自 Mock，真实识别后需保证基线准确，否则约束引擎漏检。
- **LLM 路径未实测**：planner/dialog 的 LLM Function Calling 分支无 Key 时走降级，真实调用（DeepSeek/Qwen）尚未联网验证，function 参数解析边界未测。

## P1（影响体验/精度，建议下一阶段）
- **布局为简单网格排布**：generator 仅沿 x 轴错开避免重叠，未做房间语义放置（如床靠墙、沙发对电视），美观度低。
- **尺寸修改仅支持单件首件**：dialog 正则/function 命中第一类家具，未处理"主卧衣柜"vs"次卧衣柜"的区分。
- **报价为线性近似**：bom 用构件 unit_price，未绑定真实 BOM 模板用量公式（components.json 已有 bom_template 但未用于算量）。
- **约束校验为规则子集**：仅实现墙体/管井/防水/通道/衣柜深度/床侧通道，缺水电规范、采光等。

## P2（增强项，可后置）
- **渲染为 Mock**：render 仅返回描述，无 Three.js/云渲染。
- **无撤销/快照**：对话修改不可回退。
- **无前端页面**：当前仅 Flask API，无上传/3D/对话 UI。
- **风格迁移/参考图**：未实现（F5，规划在第 14 周后）。

## 已验证通过
- generate→约束→对话→算量 闭环（Mock 路径）全绿；
- 约束引擎在 generate/dialog 必经路径；
- LLM 仅推理不编尺寸（设计契约，代码已落实：planner 输出语义层、generator 用构件真实尺寸）；
- 无密钥硬编码。
